# Test Environment Configuration

# Node Environment
NODE_ENV=test

# Application
PORT=3001
API_PREFIX=api

# Database (Test Database)
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_USERNAME=postgres
DATABASE_PASSWORD=postgres
DATABASE_NAME=mes_test

# JWT Configuration
JWT_SECRET=test-jwt-secret-key-for-integration-tests
JWT_ACCESS_TOKEN_EXPIRATION=15m
JWT_REFRESH_TOKEN_EXPIRATION=7d

# Redis (Can use real Redis or mock)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# Keycloak (Mocked in tests)
KEYCLOAK_REALM=mes-test
KEYCLOAK_CLIENT_ID=mes-backend-test
KEYCLOAK_CLIENT_SECRET=test-secret
KEYCLOAK_AUTH_SERVER_URL=http://localhost:8080

# OpenFGA (Mocked in tests - ULID format required)
OPENFGA_STORE_ID=01ARZ3NDEKTSV4RRFFQ69G5FAV
OPENFGA_API_URL=http://localhost:8082
OPENFGA_API_TOKEN=test-token

# Rate Limiting
RATE_LIMIT_TTL=60
RATE_LIMIT_LIMIT=100

# Logging
LOG_LEVEL=error

# Test Specific
TEST_TIMEOUT=30000
TEST_RETRIES=2